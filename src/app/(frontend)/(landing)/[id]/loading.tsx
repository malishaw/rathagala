/**
 * This route immediately does a server-side redirect to /ads/[id].
 * The loading state is never actually shown, but Next.js requires the file.
 */
export default function Loading() {
  return null;
}
